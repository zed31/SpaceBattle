//
// Created by 49844 on 06/11/2017.
//

#include <thread>
#include "gtest/gtest.h"

TEST(Net_test, simple_connection) {
    //c.connect([](tcp::socket &&){ std::cout << "Success" << std::endl; }, [&](std::error_code er) { std::cout << er.message() << std::endl; c.close(); });
    //std::thread t{ [&] () { io_service.run(); } };
    //c.close();
    //t.join();
}